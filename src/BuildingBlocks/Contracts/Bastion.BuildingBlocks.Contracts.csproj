<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Vocabulario de CONTRATO compartido por todos los módulos (§12): cómo se pide una colección y
    cómo vuelve —`Paginacion`, `PaginaDe`, `Orden`, `Recorrido` y `TramoDe`— y, desde el ítem 1.5,
    cómo viaja una dirección postal: `DireccionDto`, los seis campos de R17.

    `DireccionDto` llegó aquí por el mismo camino que la paginación en el 1.3: estaba en
    `Organizacion.Contracts`, la necesitaba también Terceros, y un módulo no puede referenciar el
    `Contracts` de otro para tomar prestado un DTO común —eso lo ataría a un módulo con el que no
    tiene nada que ver—. La alternativa era una segunda copia de los seis campos con los mismos
    topes, o sea dos que se separan el día que una se toque.

    Cuarto bloque común, y el inventario decía que eran tres «y no van a crecer con las fases».
    Resultó ser una predicción y no una regla: la línea se actualizó en el ítem 1.3, que es cuando
    el cuarto hizo falta.

    Y desde el ítem 1.11, cómo vuelve una importación: `InformeDeImportacionDto` y la lista cerrada
    de `MotivoDeRechazo` (ADR-0034 §2). Por la misma razón que la paginación: el informe tiene la
    misma forma para terceros hoy y para artículos mañana, y el frontal traduce los motivos con UNA
    tabla; con un informe por módulo, la tabla serían dos que se separan.

    NO referencia NADA, igual que `BuildingBlocks.Domain`, y por el mismo motivo elevado un grado:
    esto lo referencia el `Contracts` de cada módulo, o sea lo único que un módulo enseña a otro.
    Si arrastrara dominio o infraestructura, todo módulo que leyera un contrato ajeno lo vería por
    transitividad, que es justo lo que la regla 1 del §4 impide. El detalle, en el ADR-0029.

    Es de contrato y no de dominio: `Paginacion` no tiene invariantes que defender ni comportamiento
    —los números los valida el borde, que es quien los enlaza—, así que ponerla en
    `BuildingBlocks.Domain` la habría metido donde no vive.
  -->

</Project>
