<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tests unitarios del módulo Organización (§13: dominio puro, sin infraestructura y sin E/S).
    Referencia solo al dominio del módulo, que a su vez solo referencia al dominio base: si
    algún día hiciera falta un doble de algo aquí, sería la señal de que al dominio se le ha
    colado una dependencia.
  -->

  <ItemGroup Label="Sujeto de prueba">
    <ProjectReference Include="../../src/Modules/Organizacion/Bastion.Organizacion.Domain/Bastion.Organizacion.Domain.csproj" />
    <!--
      Contracts ADEMÁS del dominio, y solo para un test: los DTO repiten como números unos
      límites que manda el dominio, porque Contracts no puede referenciarlo. Este proyecto ve
      los dos y comprueba que siguen diciendo lo mismo.
    -->
    <ProjectReference Include="../../src/Modules/Organizacion/Bastion.Organizacion.Contracts/Bastion.Organizacion.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup Label="Marco de pruebas">
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
